## Context

博客文章较长（如游戏架构系列），用户浏览到底部后需要滚回顶部。添加一个固定在页面右下角的浮动按钮。

## Goals / Non-Goals

**Goals:**
- 点击按钮平滑滚动到页面顶部
- 仅在滚动超过 300px 后显示

**Non-Goals:**
- 不在首页/标签页等列表页显示（列表较短，不需要）

## Decisions

### D1: 浮动按钮定位

选择: `fixed bottom-8 right-8`，文章详情页专属
理由: 右下角不遮挡内容，位置固定易找
